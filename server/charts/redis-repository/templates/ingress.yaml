{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "redis-repository.fullname" . }}-ingress
  namespace: {{ .Values.namespace | default .Release.Namespace }}
  labels:
    {{- include "redis-repository.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.ingress.annotations | nindent 4 }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: redis-repository.{{ .Values.namespace}}.{{ .Values.ingress.hostBase }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "redis-repository.fullname" . }}
                port:
                  number: {{ .Values.service.port }}
  tls:
    - hosts:
        - redis-repository.{{ .Values.namespace}}.{{ .Values.ingress.hostBase }}
      secretName: {{ .Values.ingress.tlsSecret }}
{{- end }}
